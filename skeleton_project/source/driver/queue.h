#pragma once

typedef struct {
    int floor;
    bool up;
    bool down;
} Queue;

array queue[
    [floor, up, down],
    [floor, up, down],
    [floor, up, down],
    [floor, up, down],
]


void queue_init(Queue* queue);